FreeType
--------

  This example demonstrates Woopsi's ability to use TrueType fonts via the
  FreeType library.
  
  To run the example, copy the "default.ttf" font to the root of your DS' flash
  cart and load the ROM.


Building the Example
--------------------

  To build this example, open a shell, navigate to this directory, and type
  "make".